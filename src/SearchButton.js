import React from 'react';

function SearchButton() {

    return (
        <button 
            type='submit'
            data-testid="SearchButton"
        >
            Search
        </button>
    );
};

export default SearchButton;